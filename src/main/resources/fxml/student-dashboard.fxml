<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<BorderPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.ucop.edu.controller.StudentDashboardController"
            prefWidth="1200" prefHeight="800">

    <!-- ================= TOP BAR ================= -->
    <top>
        <HBox spacing="12" style="-fx-background-color: #0f172a;">
            <padding>
                <Insets top="18" right="18" bottom="18" left="18"/>
            </padding>

            <VBox spacing="2">
                <Label text="UCOP Education" style="-fx-text-fill: white;">
                    <font><Font size="18"/></font>
                </Label>
                <Label fx:id="welcomeLabel"
                       text="Xin chào, Student"
                       style="-fx-text-fill: #cbd5e1;"/>
            </VBox>

            <Region HBox.hgrow="ALWAYS"/>

            <Button text="Đăng xuất" onAction="#handleLogout"
                    style="-fx-background-color: transparent;
                           -fx-border-color: #94a3b8;
                           -fx-text-fill: white;
                           -fx-border-radius: 8;
                           -fx-background-radius: 8;
                           -fx-padding: 8 14;
                           -fx-cursor: hand;"/>
        </HBox>
    </top>

    <!-- ================= LEFT MENU ================= -->
    <left>
        <VBox prefWidth="220"
              spacing="10"
              style="-fx-background-color: #f8fafc;
                     -fx-border-color: #e2e8f0;
                     -fx-border-width: 0 1 0 0;">

            <padding>
                <Insets top="16" right="16" bottom="0" left="16"/>
            </padding>

            <Label text="Menu" style="-fx-text-fill: #0f172a;">
                <font><Font size="14"/></font>
            </Label>

            <!-- Courses -->
            <Button text="📚 Danh sách khóa học"
                    onAction="#showCourses"
                    style="-fx-background-color: white;
                           -fx-border-color: #e2e8f0;
                           -fx-background-radius: 10;
                           -fx-border-radius: 10;
                           -fx-padding: 10 12;
                           -fx-cursor: hand;"/>

            <!-- Cart + Badge -->
            <Button onAction="#showCart"
                    style="-fx-background-color: white;
                           -fx-border-color: #e2e8f0;
                           -fx-background-radius: 10;
                           -fx-border-radius: 10;
                           -fx-padding: 10 12;
                           -fx-cursor: hand;">
                <graphic>
                    <HBox spacing="8" alignment="CENTER_LEFT">
                        <Label text="🛒 Giỏ hàng"/>
                        <Label fx:id="cartBadgeLabel"
                               text="0"
                               visible="false"
                               managed="false"
                               style="-fx-background-color: #ef4444;
                                      -fx-text-fill: white;
                                      -fx-font-weight: bold;
                                      -fx-padding: 2 8;
                                      -fx-background-radius: 999;"/>
                    </HBox>
                </graphic>
            </Button>

            <!-- ✅ NEW: Payment -->
            <Button text="💳 Thanh toán"
                    onAction="#showPayment"
                    style="-fx-background-color: white;
                           -fx-border-color: #e2e8f0;
                           -fx-background-radius: 10;
                           -fx-border-radius: 10;
                           -fx-padding: 10 12;
                           -fx-cursor: hand;"/>

            <!-- ✅ NEW: Wallet -->
            <Button text="👛 Ví của tôi"
                    onAction="#showWallet"
                    style="-fx-background-color: white;
                           -fx-border-color: #e2e8f0;
                           -fx-background-radius: 10;
                           -fx-border-radius: 10;
                           -fx-padding: 10 12;
                           -fx-cursor: hand;"/>

            <!-- Orders -->
            <Button text="🧾 Đơn hàng của tôi"
                    onAction="#showOrders"
                    style="-fx-background-color: white;
                           -fx-border-color: #e2e8f0;
                           -fx-background-radius: 10;
                           -fx-border-radius: 10;
                           -fx-padding: 10 12;
                           -fx-cursor: hand;"/>

            <!-- ✅ NEW: Return/Refund Request -->
            <Button text="← Hoàn tiền / Hoàn đổi"
                    onAction="#showReturnRequests"
                    style="-fx-background-color: white;
                           -fx-border-color: #e2e8f0;
                           -fx-background-radius: 10;
                           -fx-border-radius: 10;
                           -fx-padding: 10 12;
                           -fx-cursor: hand;"/>
            
            		<Region VBox.vgrow="ALWAYS"/>

            <!-- Footer tip -->
            <HBox style="-fx-background-color: #f8fafc;
                         -fx-border-color: #e2e8f0;
                         -fx-border-width: 1 0 0 0;
                         -fx-padding: 10 0 12 0;">
                <Label text="Tip: Chọn khóa học rồi bấm icon 🛒 để thêm vào giỏ."
                       wrapText="true"
                       style="-fx-text-fill: #64748b;"/>
            </HBox>

        </VBox>
    </left>

    <!-- ================= CENTER CONTENT ================= -->
    <center>
        <StackPane fx:id="contentPane"
                   style="-fx-background-color: white;"/>
    </center>

</BorderPane>
