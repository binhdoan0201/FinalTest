<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.ucop.edu.controller.DuyetDonStaffController"
            style="-fx-background-color: #eef2f7;">

    <!-- TOP BAR -->
    <top>
        <HBox spacing="10" alignment="CENTER_LEFT"
              style="-fx-background-color: white;
                     -fx-padding: 12 14;
                     -fx-border-color: #e5e7eb;
                     -fx-border-width: 0 0 1 0;">
            <Label text="📌 DANH SÁCH ORDER PENDING"
                   style="-fx-font-size: 18px;
                          -fx-font-weight: 900;
                          -fx-text-fill: #0f172a;"/>

            <Pane HBox.hgrow="ALWAYS"/>

            <Button text="Duyệt tất cả" onAction="#handleApproveAll"
                    style="-fx-background-color: #ef4444;
                           -fx-text-fill: white;
                           -fx-font-weight: 800;
                           -fx-background-radius: 10;
                           -fx-padding: 8 14;
                           -fx-cursor: hand;"/>

            <Button text="Refresh" onAction="#handleRefresh"
                    style="-fx-background-color: #2563eb;
                           -fx-text-fill: white;
                           -fx-font-weight: 800;
                           -fx-background-radius: 10;
                           -fx-padding: 8 14;
                           -fx-cursor: hand;"/>
                           <Button text="Logout" onAction="#handleLogout"
        style="-fx-background-color: #0f172a;
               -fx-text-fill: white;
               -fx-font-weight: 800;
               -fx-background-radius: 10;
               -fx-padding: 8 14;
               -fx-cursor: hand;"/>
        </HBox>
    </top>

    <!-- CENTER CONTENT -->
    <center>
        <VBox spacing="10" style="-fx-padding: 14;">
            <!-- CARD -->
            <VBox spacing="10"
                  style="-fx-background-color: white;
                         -fx-background-radius: 14;
                         -fx-padding: 14;
                         -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.10), 14, 0, 0, 2);">

                <Label text="🧾 Orders cần xử lý"
                       style="-fx-text-fill: #334155;
                              -fx-font-weight: 800;
                              -fx-font-size: 13px;"/>

                <TableView fx:id="tblOrders" VBox.vgrow="ALWAYS"
                           style="-fx-background-color: transparent;
                                  -fx-background-radius: 12;
                                  -fx-border-color: #e5e7eb;
                                  -fx-border-radius: 12;">

                    <!-- FIX lỗi columnResizePolicy giống bạn gặp trước -->
                    <columnResizePolicy>
                        <TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/>
                    </columnResizePolicy>

                    <columns>
                        <TableColumn fx:id="colId" text="ID" prefWidth="80"/>
                        <TableColumn fx:id="colStudent" text="Student" prefWidth="220"/>
                        <TableColumn fx:id="colCreatedAt" text="Created At" prefWidth="180"/>
                        <TableColumn fx:id="colTotal" text="Total (VND)" prefWidth="160"/>
                        <TableColumn fx:id="colStatus" text="Status" prefWidth="120"/>
                        <TableColumn fx:id="colAction" text="Action" prefWidth="140"/>
                    </columns>
                </TableView>

                <!-- MESSAGE -->
                <Label fx:id="lblMsg"
                       style="-fx-text-fill: #dc2626;
                              -fx-font-weight: 800;"/>
            </VBox>

            <!-- FOOT NOTE -->
            <Label text="💡 Tip: Chỉ Order đang PENDING mới bấm duyệt được."
                   style="-fx-text-fill: #64748b; -fx-font-weight: 700;"/>
        </VBox>
    </center>

</BorderPane>
